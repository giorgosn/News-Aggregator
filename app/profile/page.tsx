import Layout from '../layout';
import ProfileContent from './ProfileContent';

const ProfilePage = () => {
  return (
    <Layout>
      <ProfileContent />
    </Layout>
  );
};

export default ProfilePage;